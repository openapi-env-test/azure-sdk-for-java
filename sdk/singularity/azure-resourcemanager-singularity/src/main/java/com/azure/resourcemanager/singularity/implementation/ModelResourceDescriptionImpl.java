// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.singularity.implementation;

import com.azure.core.util.Context;
import com.azure.resourcemanager.singularity.fluent.models.ModelResourceDescriptionInner;
import com.azure.resourcemanager.singularity.models.ModelResourceDescription;
import com.azure.resourcemanager.singularity.models.ModelResourcePatchDescription;
import com.azure.resourcemanager.singularity.models.ResourceIdentity;
import com.azure.resourcemanager.singularity.models.ResourceSystemData;
import java.util.Collections;
import java.util.Map;

public final class ModelResourceDescriptionImpl
    implements ModelResourceDescription, ModelResourceDescription.Definition, ModelResourceDescription.Update {
    private ModelResourceDescriptionInner innerObject;

    private final com.azure.resourcemanager.singularity.SingularityManager serviceManager;

    public String id() {
        return this.innerModel().id();
    }

    public String name() {
        return this.innerModel().name();
    }

    public String type() {
        return this.innerModel().type();
    }

    public ResourceIdentity identity() {
        return this.innerModel().identity();
    }

    public Map<String, String> tags() {
        Map<String, String> inner = this.innerModel().tags();
        if (inner != null) {
            return Collections.unmodifiableMap(inner);
        } else {
            return Collections.emptyMap();
        }
    }

    public ResourceSystemData systemData() {
        return this.innerModel().systemData();
    }

    public String resourceGroupName() {
        return resourceGroupName;
    }

    public ModelResourceDescriptionInner innerModel() {
        return this.innerObject;
    }

    private com.azure.resourcemanager.singularity.SingularityManager manager() {
        return this.serviceManager;
    }

    private String resourceGroupName;

    private String accountName;

    private String modelName;

    private ModelResourcePatchDescription updateBody;

    public ModelResourceDescriptionImpl withExistingAccount(String resourceGroupName, String accountName) {
        this.resourceGroupName = resourceGroupName;
        this.accountName = accountName;
        return this;
    }

    public ModelResourceDescription create() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getModels()
                .createOrUpdate(resourceGroupName, accountName, modelName, this.innerModel(), Context.NONE);
        return this;
    }

    public ModelResourceDescription create(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getModels()
                .createOrUpdate(resourceGroupName, accountName, modelName, this.innerModel(), context);
        return this;
    }

    ModelResourceDescriptionImpl(String name, com.azure.resourcemanager.singularity.SingularityManager serviceManager) {
        this.innerObject = new ModelResourceDescriptionInner();
        this.serviceManager = serviceManager;
        this.modelName = name;
    }

    public ModelResourceDescriptionImpl update() {
        this.updateBody = new ModelResourcePatchDescription();
        return this;
    }

    public ModelResourceDescription apply() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getModels()
                .update(resourceGroupName, accountName, modelName, updateBody, Context.NONE);
        return this;
    }

    public ModelResourceDescription apply(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getModels()
                .update(resourceGroupName, accountName, modelName, updateBody, context);
        return this;
    }

    ModelResourceDescriptionImpl(
        ModelResourceDescriptionInner innerObject,
        com.azure.resourcemanager.singularity.SingularityManager serviceManager) {
        this.innerObject = innerObject;
        this.serviceManager = serviceManager;
        this.resourceGroupName = Utils.getValueFromIdByName(innerObject.id(), "resourcegroups");
        this.accountName = Utils.getValueFromIdByName(innerObject.id(), "accounts");
        this.modelName = Utils.getValueFromIdByName(innerObject.id(), "models");
    }

    public ModelResourceDescription refresh() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getModels()
                .getWithResponse(resourceGroupName, accountName, modelName, Context.NONE)
                .getValue();
        return this;
    }

    public ModelResourceDescription refresh(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getModels()
                .getWithResponse(resourceGroupName, accountName, modelName, context)
                .getValue();
        return this;
    }

    public void validate(ModelResourceDescriptionInner body) {
        serviceManager.models().validate(resourceGroupName, accountName, modelName, body);
    }

    public void validate(ModelResourceDescriptionInner body, Context context) {
        serviceManager.models().validate(resourceGroupName, accountName, modelName, body, context);
    }

    public ModelResourceDescriptionImpl withTags(Map<String, String> tags) {
        if (isInCreateMode()) {
            this.innerModel().withTags(tags);
            return this;
        } else {
            this.updateBody.withTags(tags);
            return this;
        }
    }

    public ModelResourceDescriptionImpl withIdentity(ResourceIdentity identity) {
        this.innerModel().withIdentity(identity);
        return this;
    }

    public ModelResourceDescriptionImpl withSystemData(ResourceSystemData systemData) {
        if (isInCreateMode()) {
            this.innerModel().withSystemData(systemData);
            return this;
        } else {
            this.updateBody.withSystemData(systemData);
            return this;
        }
    }

    private boolean isInCreateMode() {
        return this.innerModel().id() == null;
    }
}

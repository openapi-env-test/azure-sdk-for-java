# NOTE: Please refer to https://aka.ms/azsdk/engsys/ci-yaml before editing this file.

trigger:
  branches:
    include:
      - master
      - hotfix/*
      - release/*
  paths:
    include:
      - sdk/contoso_cdn/

pr:
  branches:
    include:
      - master
      - feature/*
      - hotfix/*
      - release/*
  paths:
    include:
      - sdk/contoso_cdn/

extends:
  template: ../../eng/pipelines/templates/stages/archetype-sdk-client.yml
  parameters:
    ServiceDirectory: contoso_cdn
    Artifacts:
      - name: azure-resourcemanager-contoso_cdn
        groupId: com.azure.resourcemanager
        safeName: azureresourcemanagercontoso_cdn
